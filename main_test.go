package main

import (
	"testing"
)

func TestSmoke(t *testing.T) {
	//r := strings.NewReader("abc")
	//checksummer := NewMackerrasChecksum(r, 1)
	//assert.True(t, checksummer.Consume(), "Consume error")
	//c1 := checksummer.Checksum()
	//assert.True(t, checksummer.Consume(), "Consume error")
	//c2 := checksummer.Checksum()
	//assert.True(t, checksummer.Consume(), "Consume error")
	//c3 := checksummer.Checksum()
	//assert.False(t, checksummer.Consume(), "Consume unexpectedly succeeded")
	//
	//assert.NotEqual(t, c1, c2, "c1 == c2")
	//assert.NotEqual(t, c2, c3, "c2 == c3")
	//assert.NotEqual(t, c1, c3, "c1 == c3")
}

